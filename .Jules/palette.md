## 2024-05-18 - Missing ARIA labels
**Learning:** The application uses many icon-only buttons (like those in VideoCardYT, SideNav, BottomNav) that lack ARIA labels, making them inaccessible to screen readers.
**Action:** Add aria-label attributes to icon-only buttons to improve accessibility.
